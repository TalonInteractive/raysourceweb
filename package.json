{
  "dependencies": {
    "@tailwindcss/cli": "^4.2.2"
  }
}

{
  "scripts": {
    "dev": "npx @tailwindcss/cli -i main.css -o assets/output.css --watch",
    "build": "npx @tailwindcss/cli -i main.css -o assets/output.css --minify"
  }
}
